#import "../fragments/games-basic.gql"

query games ($locale: I18NLocaleCode!) {
  games(locale: $locale) {
    data {
      id
      attributes {
        ...GameBasic
      }
    }
    meta {
      pagination {
        total
      }
    }
  }
} 
