#import "./game-gallery.gql"

fragment GameBasic on GameEntityResponseCollection {
  data {
    id
    attributes {
      name
      slug
      summary
      # TODO use filter here gallery()
      gallery {
        data {
          attributes {
            ...GameGallery
          }
        }
      }
    }
  }
}
